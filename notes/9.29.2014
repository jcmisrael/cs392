#ifndef MAX
#define MAX 1024
#endif

int fd = open(char*, flags[, mode]);
int n = read(fd, void* buff, MAX-1);
buff[n] = '\0';			/* If you read from a keyboard, read from MAX-1 and add '\0', otherwise dont
				   Read will return -1 when it cant read the file (bad permissions on fd, someone erases it mid-read. */
int n = write(fd, void* buff, size); /* Returns number of bytes write. */
int offset = lseek(fd, amount, direction);
int rc = close(fd);		/* (void)close(fd); casts return type as void*/

/* 
GCC can compile almost anything, stands for GNU Compiler Collection

Compiler process
1) Lexical analysis
2) Parsing => Parse Tree
3) Parse Tree => RTL (Run-Time-Langauge)
4) RTL optimization
5) RTL => Assembly
6) Assembly => Object
7) Linking

gcc flags
-std=gnu89 = addons made for gnu, not reccomended
-pedantic-errors -Wall

Helpful macros
__FILE__
__FUNCTION__
__LINE__
__DATE__
__TIME__

Extensions:
C: .c
C++: .cpp .cxx .cc .C .c++
headers: .h
Assembly: .S
Not to the prepocessor: 
-C: .i
-C++: ii
-Assembly: .s
Object Files: .o
Static Obj Library: .a
Shared Object: .so
Stop compiling at:
-c Create OBJ and stop
-e Preprocess and stop
*/
